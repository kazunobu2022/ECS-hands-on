  sudo yum update
  sudo yum install -y docker
  sudo systemctl start docker
  sudo usermod -a -G docker ec2-user
  sudo systemctl enable docker
  sudo curl -L https://github.com/docker/compose/releases/download/1.28.5/docker-compose-`uname -s`-`uname -m` -o /usr/local/bin/docker-compose
  sudo chmod +x /usr/local/bin/docker-compose
  sudo mkdir docker
  cd docker/
  sudo vi dockerfile
  sudo rm dockerfile 
  sudo vi Dockerfile
  mkdir src
  sudo mkdir src
  cd src/
  sudo vi index.html
  cd ..
  sudo su
  docker build -t 744195504551.dkr.ecr.ap-northeast-1.amazonaws.com/hello-world/hello-world:latest .
  sudo su
  ls
  aws ecr get-login-password --region ap-northeast-1 | docker login --username AWS --password-stdin 744195504551.dkr.ecr.ap-northeast-1.amazonaws.com
  sudo su
  aws ecr get-login-password --region ap-northeast-1 | docker login --username AWS --password-stdin 744195504551.dkr.ecr.ap-northeast-1.amazonaws.com
  docker build -t hello-world .
  sudo su
  ls
  cd docker/
  ls
  sudo vi src/
  ls
  cd src/
  ls
  sudo vi index.html 
  aws ecr get-login-password --region ap-northeast-1 | docker login --username AWS --password-stdin 744195504551.dkr.ecr.ap-northeast-1.amazonaws.com
  cd ..
  ls
  docker build -t hello-world .
  docker tag hello-world:latest 744195504551.dkr.ecr.ap-northeast-1.amazonaws.com/hello-world:latest
  docker push 744195504551.dkr.ecr.ap-northeast-1.amazonaws.com/hello-world:latest
  history 
  history > command.txt
  sudo su
  git clone  Authentication failed for 'https://git-codecommit.ap-northeast-1.amazonaws.com/v1/repos/ecs-hands-on/'
  it config --global user.name "takakabe"
  git config --global user.name "takakabe"
  git config --global user.email kabegiwablog@example.com
  ssh-keygen -t rsa
  cd ~/.ssh/
  ks
  ls
  cat id_rsa.pub 
  cd ~/
  ls
  cd ECS-hands-on/
  git push
  git add -A
  git config --global --add safe.directory /home/ec2-user/ECS-hands-on
  git add -A
  sudo su
  ls
  cd dir/
  ls
  cut -c 5-99 > test
  cat command.txt | cut -c 5-99 
  cat command.txt | cut -c 6-99 
  cat command.txt 
  cat command.txt | cut -c 6-9999
  cat command.txt | cut -c 6-9999 > command.txt 
  cat command.txt 
  ls
  cat command.txt 
  cd te
  cat test 
  cat command.txt 
  ls
  cat sudocommand.txt 
  ls
  history 
  history 1-100
  history > command.txt 
